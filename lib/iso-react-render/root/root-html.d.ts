declare type Opts = {
  styletron: any;
  jsonGlobals: any;
  reactMarkup: string;
  clientScript: string;
};
export declare function rootHtml({
  styletron,
  jsonGlobals,
  reactMarkup,
  clientScript,
}: Opts): string;
export {};
